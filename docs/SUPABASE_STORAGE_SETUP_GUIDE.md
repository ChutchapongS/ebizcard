# 📚 คู่มือการตั้งค่า Supabase Storage สำหรับรูป Profile

## 🎯 ภาพรวม

เอกสารนี้จะแนะนำวิธีการตั้งค่า Supabase Storage เพื่อให้สามารถบันทึกรูปภาพ profile ได้อย่างถูกต้อง แทนที่จะใช้วิธี base64 fallback

---

## 📋 ปัญหาที่พบ

**ปัจจุบัน**: ระบบไม่สามารถอัปโหลดรูปไปยัง Supabase Storage ได้ จึงใช้วิธี fallback คือเก็บรูปเป็น base64 ใน user metadata

**ข้อจำกัด**:
- ❌ Base64 มีขนาดใหญ่ (เพิ่ม ~33% จากขนาดไฟล์จริง)
- ❌ ทำให้ user metadata มีขนาดใหญ่เกินไป
- ❌ มีข้อจำกัดในการอัปเดต (payload size limit)
- ❌ ประสิทธิภาพการโหลดช้า

---

## ✅ วิธีแก้ไข (3 ขั้นตอน)

### 🔧 ขั้นตอนที่ 1: รัน Migration

มี 2 วิธี เลือกวิธีใดวิธีหนึ่ง:

#### วิธีที่ 1: ผ่าน Supabase CLI (แนะนำ)

```bash
# ตรวจสอบว่ามี Supabase CLI หรือยัง
supabase --version

# ถ้ายังไม่มี ติดตั้งก่อน
npm install -g supabase

# Link โปรเจกต์กับ Supabase
supabase link --project-ref YOUR_PROJECT_REF

# รัน migrations ทั้งหมด
supabase db push

# หรือรันเฉพาะ migration นี้
supabase migration up 010_fix_storage_buckets
```

#### วิธีที่ 2: ผ่าน Supabase Dashboard

1. ไปที่ https://supabase.com/dashboard
2. เลือกโปรเจกต์ของคุณ
3. ไปที่ **SQL Editor**
4. คัดลอกเนื้อหาจากไฟล์ `scripts/setup-supabase-storage.sql`
5. Paste ลงใน SQL Editor
6. กดปุ่ม **"Run"** หรือ **F5**

---

### 🔍 ขั้นตอนที่ 2: ตรวจสอบผลลัพธ์

#### ใน Supabase Dashboard:

1. **ตรวจสอบ Buckets**:
   - ไปที่ **Storage** → **Buckets**
   - ควรเห็น 3 buckets:
     - ✅ `business-cards` (public)
     - ✅ `avatars` (public)
     - ✅ `logos` (public)

2. **ตรวจสอบ Policies**:
   - คลิกที่แต่ละ bucket
   - ไปที่แท็บ **Policies**
   - ควรเห็น 4 policies ต่อ bucket:
     - ✅ Upload (INSERT) - สำหรับผู้ใช้ที่เข้าสู่ระบบ
     - ✅ Update (UPDATE) - สำหรับผู้ใช้ที่เข้าสู่ระบบ
     - ✅ Delete (DELETE) - สำหรับผู้ใช้ที่เข้าสู่ระบบ
     - ✅ View (SELECT) - สำหรับทุกคน (public)

#### ผ่าน SQL:

```sql
-- ตรวจสอบ buckets
SELECT 
  id,
  name,
  public,
  file_size_limit / 1024 / 1024 as max_size_mb,
  allowed_mime_types
FROM storage.buckets 
WHERE id IN ('business-cards', 'avatars', 'logos');

-- ตรวจสอบ policies
SELECT 
  policyname,
  cmd,
  roles
FROM pg_policies
WHERE schemaname = 'storage' 
  AND tablename = 'objects'
  AND (
    policyname LIKE '%business card%' OR
    policyname LIKE '%avatar%' OR
    policyname LIKE '%logo%'
  );
```

---

### 🧪 ขั้นตอนที่ 3: ทดสอบการอัปโหลด

1. **ทดสอบผ่าน Web App**:
   ```bash
   cd apps/web
   npm run dev
   ```

2. เปิด Browser ไปที่ http://localhost:3000
3. เข้าสู่ระบบ
4. ไปที่หน้า **Settings**
5. คลิก "เลือกรูปภาพ" และอัปโหลดรูป
6. ตรวจสอบ Console (F12):
   - ✅ ควรเห็น: `"✅ Upload successful to bucket: avatars"` (หรือ business-cards)
   - ✅ ควรเห็น URL ที่เริ่มต้นด้วย `https://[project].supabase.co/storage/v1/object/public/...`

7. **ตรวจสอบใน Supabase Dashboard**:
   - ไปที่ **Storage** → เลือก bucket → **Files**
   - ควรเห็นไฟล์รูปที่อัปโหลดไป

---

## 📊 ข้อมูลเทคนิค

### Buckets Configuration:

| Bucket | Public | Max Size | Allowed Types |
|--------|--------|----------|---------------|
| `business-cards` | ✅ Yes | 50 MB | image/jpeg, png, gif, webp, svg |
| `avatars` | ✅ Yes | 50 MB | image/jpeg, png, gif, webp, svg |
| `logos` | ✅ Yes | 50 MB | image/jpeg, png, gif, webp, svg |

### Storage Policies:

**สำหรับผู้ใช้ที่เข้าสู่ระบบ (authenticated)**:
- ✅ อัปโหลดไฟล์ได้
- ✅ แก้ไขไฟล์ได้
- ✅ ลบไฟล์ได้

**สำหรับทุกคน (public)**:
- ✅ ดูไฟล์ได้ (เพื่อให้รูป profile แสดงได้)

---

## 🔧 การแก้ปัญหา

### ปัญหา 1: Bucket ไม่ถูกสร้าง

**สาเหตุ**: อาจมี bucket ชื่อเดียวกันอยู่แล้ว

**วิธีแก้**:
```sql
-- ตรวจสอบ buckets ที่มีอยู่
SELECT * FROM storage.buckets;

-- ลบ bucket เก่า (ระวัง: จะลบไฟล์ทั้งหมด!)
DELETE FROM storage.buckets WHERE id = 'business-cards';

-- สร้าง bucket ใหม่อีกครั้ง
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES ('business-cards', 'business-cards', true, 52428800, 
  ARRAY['image/jpeg', 'image/jpg', 'image/png', 'image/gif', 'image/webp']);
```

### ปัญหา 2: Policies ไม่ทำงาน

**สาเหตุ**: อาจมี policy ซ้ำกันหรือขัดแย้งกัน

**วิธีแก้**:
```sql
-- ลบ policies ทั้งหมดของ bucket
DROP POLICY IF EXISTS "Users can upload avatar files" ON storage.objects;
DROP POLICY IF EXISTS "Users can update avatar files" ON storage.objects;
DROP POLICY IF EXISTS "Users can delete avatar files" ON storage.objects;
DROP POLICY IF EXISTS "Anyone can view avatar files" ON storage.objects;

-- สร้าง policies ใหม่อีกครั้ง
-- (ใช้โค้ดจาก setup-supabase-storage.sql)
```

### ปัญหา 3: ยังอัปโหลดไม่ได้

**ตรวจสอบ**:

1. **Environment Variables**:
   ```bash
   # ใน apps/web/.env.local
   NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
   ```

2. **User Authentication**:
   ```javascript
   // ตรวจสอบใน Console
   const { data: { user } } = await supabase.auth.getUser();
   console.log('Current user:', user);
   ```

3. **Network/CORS**:
   - ตรวจสอบใน DevTools → Network tab
   - ดูว่ามี CORS errors หรือไม่

4. **Supabase Service**:
   - ตรวจสอบที่ https://status.supabase.com
   - ดูว่า Storage service ทำงานปกติหรือไม่

---

## 🎯 ผลลัพธ์ที่คาดหวัง

หลังจากตั้งค่าเสร็จสิ้น:

### ก่อนการแก้ไข:
```javascript
// ❌ อัปโหลดล้มเหลว → ใช้ base64 fallback
{
  avatar_url: "data:image/png;base64,iVBORw0KGgoAAAANSUhEUg..." // ยาวมาก!
}
```

### หลังการแก้ไข:
```javascript
// ✅ อัปโหลดสำเร็จ → ใช้ Supabase Storage URL
{
  avatar_url: "https://project.supabase.co/storage/v1/object/public/avatars/profiles/profile-xxx.jpg"
}
```

### ประโยชน์:
- ✅ ประหยัดพื้นที่ใน database
- ✅ โหลดรูปเร็วขึ้น (CDN)
- ✅ จัดการไฟล์ง่ายขึ้น
- ✅ รองรับรูปขนาดใหญ่ได้
- ✅ สามารถใช้ร่วมกับ Image Transformation API ของ Supabase

---

## 📞 ติดต่อและสนับสนุน

หากยังมีปัญหาหรือต้องการความช่วยเหลือ:

1. ตรวจสอบ [Supabase Storage Documentation](https://supabase.com/docs/guides/storage)
2. ดู Logs ใน Supabase Dashboard → **Logs** → **Storage Logs**
3. ตรวจสอบ Console logs ใน Browser DevTools

---

## 📝 หมายเหตุเพิ่มเติม

- **ความปลอดภัย**: ระบบใช้ Row Level Security (RLS) เพื่อควบคุมการเข้าถึง
- **ประสิทธิภาพ**: Supabase Storage ใช้ CDN ทำให้โหลดรูปเร็วขึ้น
- **ราคา**: Supabase มี Free tier ให้ 1 GB storage
- **Backup**: ควร backup ไฟล์สำคัญเป็นประจำ

---

**เวอร์ชัน**: 1.0  
**อัปเดตล่าสุด**: 2024-10-08  
**ผู้เขียน**: AI Assistant

