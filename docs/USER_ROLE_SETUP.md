# User Role Management Setup

ระบบจัดการ Role ของ User แบบ Dynamic ที่รองรับ 3 ระดับสิทธิ์: Owner, Admin, และ User

## ระดับสิทธิ์ (User Roles)

### 👑 Owner
- สิทธิ์สูงสุดในการจัดการระบบ
- สามารถเปลี่ยน role ของ user คนอื่นได้ (รวมถึง owner คนอื่น)
- สามารถเข้าถึง admin dashboard ได้
- สามารถจัดการผู้ใช้ทั้งหมดได้

### 👨‍💼 Admin  
- สามารถจัดการ user อื่นๆ ได้ (ยกเว้น owner)
- สามารถเข้าถึง admin dashboard ได้
- ไม่สามารถเปลี่ยน role ของ owner ได้

### 👤 User
- ผู้ใช้ทั่วไป
- ไม่สามารถเข้าถึง admin dashboard ได้
- ไม่สามารถจัดการ user อื่นได้

## การติดตั้งและตั้งค่า

### 1. รัน Database Migration

```bash
# รัน migration เพื่อเพิ่มตารางและ columns ที่จำเป็น
supabase db push
```

### 2. ตั้งค่า Owner คนแรก

หลังจากที่ user คนแรกสมัครสมาชิกแล้ว ให้รันคำสั่งนี้เพื่อตั้งค่าเป็น owner:

```bash
# เปลี่ยนไปยังโฟลเดอร์ web app
cd apps/web

# รัน script ตั้งค่า owner (แทนที่ email ด้วย email จริง)
npm run setup-owner your-email@example.com
```

### 3. เข้าถึง Admin Dashboard

หลังจากตั้งค่า owner แล้ว:
1. เข้าสู่ระบบด้วยบัญชี owner
2. ไปที่ `/admin/users` เพื่อจัดการ user roles

## การใช้งาน

### เปลี่ยน Role ของ User

1. เข้าสู่ระบบด้วยบัญชี Admin หรือ Owner
2. ไปที่ `/admin/users`
3. คลิก "Change Role" ด้านข้าง user ที่ต้องการเปลี่ยน
4. เลือก role ใหม่และใส่เหตุผล (optional)
5. คลิก "Update Role"

### ข้อจำกัด

- **Owner** เท่านั้นที่สามารถเปลี่ยน role เป็น Owner ได้
- **Admin** สามารถเปลี่ยน role เป็น Admin หรือ User ได้
- ไม่สามารถ demote Owner คนสุดท้ายได้ (ต้องมี Owner อย่างน้อย 1 คน)
- ไม่สามารถเปลี่ยน role ของตัวเองได้

## Database Schema

### profiles table (เพิ่ม columns)
```sql
user_type TEXT DEFAULT 'user' CHECK (user_type IN ('owner', 'admin', 'user'))
is_active BOOLEAN DEFAULT true
role_permissions JSONB DEFAULT '{}'
assigned_by UUID REFERENCES auth.users(id)
role_updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
```

### role_changes table (ใหม่)
```sql
id UUID DEFAULT gen_random_uuid() PRIMARY KEY
user_id UUID NOT NULL REFERENCES auth.users(id)
old_role TEXT
new_role TEXT NOT NULL
changed_by UUID NOT NULL REFERENCES auth.users(id)
reason TEXT
created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
```

## API Endpoints

### GET /api/admin/update-user-type
- ดึงรายชื่อ user ทั้งหมด (Admin/Owner เท่านั้น)
- ต้องมี Authorization header

### POST /api/admin/update-user-type
- เปลี่ยน role ของ user
- Body: `{ targetUserId, newRole, reason }`
- ต้องมี Authorization header

## Security Features

- **Row Level Security (RLS)** เปิดใช้งาน
- **Audit Trail** บันทึกการเปลี่ยน role ทั้งหมด
- **Permission Check** ตรวจสอบสิทธิ์ก่อนดำเนินการ
- **Token Validation** ตรวจสอบ JWT token
- **Owner Protection** ป้องกันการ demote owner คนสุดท้าย

## Troubleshooting

### ไม่สามารถเข้าถึง Admin Dashboard ได้
- ตรวจสอบว่า user มี role เป็น Admin หรือ Owner
- ตรวจสอบการ login และ session

### ไม่สามารถเปลี่ยน role ได้
- ตรวจสอบสิทธิ์ของ user ปัจจุบัน
- ตรวจสอบว่ามี Owner อย่างน้อย 1 คน (ถ้าต้องการ demote owner)

### Script setup-owner ไม่ทำงาน
- ตรวจสอบว่า user มีการสมัครสมาชิกแล้ว
- ตรวจสอบ environment variables
- ตรวจสอบ connection กับ Supabase

## การพัฒนาต่อ

- เพิ่ม Custom Permissions
- เพิ่ม User Groups
- เพิ่ม Role-based UI Components
- เพิ่ม Email Notifications สำหรับการเปลี่ยน role
